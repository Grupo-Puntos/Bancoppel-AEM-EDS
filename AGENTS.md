# AGENTS.md

This project is a website built with Edge Delivery Services in Adobe Experience Manager Sites as a Cloud Service. As an agent, follow the instructions in this file to deliver code based on Adobe's standards for fast, easy-to-author, and maintainable web experiences.

This is the single source of truth for project context, shared by every AI coding agent (Claude Code, Cursor, Copilot, Codex, the AEM Experience Modernization Agent, etc.). `CLAUDE.md` at the repo root only contains `@AGENTS.md` — do not duplicate content there, edit this file instead.

## Project Overview

**Project name:** Bancoppel-AEM-EDS
**Repository:** https://github.com/Grupo-Puntos/Bancoppel-AEM-EDS

This project is a demo copy of the **home page** of https://www.bancoppel.com/main/index.html, built with **Adobe Experience Manager Edge Delivery Services (EDS)**, following the official documentation at https://www.aem.live/docs/.

Goal: maximum UI consistency (layout, styles, animations/transitions) with the original BanCoppel site, implemented 100% with the EDS block pattern (no build step, native JS/CSS, ESM).

This project is based on the <https://github.com/adobe/aem-boilerplate/> project and set up as a new project under `Grupo-Puntos/Bancoppel-AEM-EDS`. You are expected to follow the coding style and practices established in the boilerplate, but add functionality according to the needs of the site currently developed.

The repository provides the basic structure, blocks, and configuration needed to run a complete site with `*.aem.live` as the backend.

### ⚠️ Known limitation: source fidelity

The initial version of this project was built **without raw network/HTML/CSS access to `bancoppel.com`** (the environment used to bootstrap it only had access to a domain allowlist that excluded `bancoppel.com`, and its fetch tool returned readable text, not source code). As a result:

- Content structure, copy, links, and image *placement* were extracted from the real site and should be accurate.
- Brand colors, exact typography, animation timing, and all image/icon assets in `/icons` and `/main/img` are **original approximations**, not pixel-perfect copies of BanCoppel's real code or binaries.
- **If you (the agent) have live network access to `bancoppel.com`**, or the team has provided real brand assets/CSS/Figma files, prioritize replacing the approximations over preserving them. See "Experience Modernization Agent" below — it is specifically designed to close this kind of fidelity gap.

### Key Technologies

- Edge Delivery Services for AEM Sites (documentation at <https://www.aem.live/> – search with `site:www.aem.live` to restrict web search results)
- Vanilla JavaScript (ES6+), no transpiling, no build steps
- CSS3 with modern features, no Tailwind or other CSS frameworks
- HTML5 semantic markup generated by the aem.live backend, decorated by our code
- Node.js tooling, managed with **pnpm** (not npm — see "Package Manager" below)

## Package Manager: pnpm

This project uses **pnpm** instead of npm for all local dev tooling (lint, AEM CLI). This has **no effect on the production site** — EDS is buildless; the browser loads `scripts.js`/`aem.js`/`*.css` directly as static files. `package.json` is only used for local dev tooling.

- Lockfile: `pnpm-lock.yaml` (commit this; never commit `package-lock.json` or `yarn.lock`)
- `package.json` pins the manager via `"packageManager": "pnpm@<version>"` (respected by Corepack) — keep in sync with the version used in CI
- CI workflows (`.github/workflows/*.yaml`) must use `pnpm/action-setup` + `actions/setup-node` with `cache: pnpm`, and `pnpm install --frozen-lockfile` instead of `npm ci`

## Setup Commands

- Install dependencies: `pnpm install`
- Start local development: `pnpm dlx @adobe/aem-cli up --no-open --forward-browser-logs` (run in background, if possible)
  * Install the AEM CLI globally by running `pnpm add -g @adobe/aem-cli` then `aem up` is equivalent to the command above
  * The dev server runs at `http://localhost:3000` with auto-reload. Open it in playwright, puppeteer, or a browser. If none are available, ask the human to open it and give feedback.
- Run linting before committing: `pnpm lint`
- Auto-fix linting issues: `pnpm lint:fix`

## Project Structure

```
├── blocks/                # Reusable content blocks
│   └── {blockname}/         - Individual block directory
│       ├── {blockname}.js     # Block's JavaScript
│       └── {blockname}.css    # Block's styles
├── styles/                # Global styles and CSS
│   ├── styles.css           # Minimal global styling and layout required for LCP
│   ├── lazy-styles.css      # Additional global styling for below-the-fold/post-LCP content
│   └── fonts.css            # Font definitions
├── scripts/                # JavaScript libraries and utilities
│   ├── aem.js                # Core AEM Library for Edge Delivery page decoration logic (NEVER MODIFY THIS FILE)
│   ├── scripts.js            # Global JavaScript utilities, main entry point for page decoration
│   └── delayed.js            # Delayed functionality such as martech loading
├── fonts/                  # Web fonts
├── icons/                  # SVG icons (brand wordmark + category icons — see "BanCoppel Brand & Content Model")
├── main/                   # Home page content and its local image assets
│   ├── index.html            # Home page source (mirrors bancoppel.com/main/index.html)
│   └── img/                  # Images/illustrations referenced only by the home page
├── nav.plain.html          # Header nav fragment, fetched by blocks/header
├── footer.plain.html       # Footer fragment, fetched by blocks/footer
├── head.html                # Global HTML head content
└── 404.html                  # Custom 404 page
```

**Current block inventory for this project** (home page only, as of this version — update this list as blocks are added/removed):

| Block | Home section | Purpose |
|---|---|---|
| `header` | — | Nav bar, loads `nav.plain.html`, brand-color overrides |
| `footer` | — | Footer, loads `footer.plain.html`, brand-color overrides |
| `app-download` | Hero | "El banco que te acompaña a donde vayas" — copy + phone mockup + QR |
| `benefits` | "BanCoppel te da posibilidades" | Product cards (Tarjeta de Crédito, Cuenta Efectiva Digital, Pagaré, Seguros) |
| `promotions` | "En BanCoppel eliges tú" | Promociones/Campañas tabs with stories-style autoplay |
| `advisor` | "BanCoppel te aconseja" | Blog CTA banner |
| `security` | "Nunca solicitamos datos personales..." | Security messaging + illustration |
| `faq` | "Resolvemos tus dudas" | Accordion, 6 categories (Tarjetas de Crédito, Cuentas, Préstamos, Inversiones, Seguros, Empresas) |
| `login` | "Banca por Internet" | Username/password form |
| `phone-banner` | — (global popup) | Dismissible modal, "Mantén disponible tu línea telefónica" |

## Code Style Guidelines

### JavaScript

- Use ES6+ features (arrow functions, destructuring, etc.)
- Follow Airbnb ESLint rules (already configured)
- Always include `.js` file extensions in imports
- Use Unix line endings (LF)

### CSS

- Follow Stylelint standard configuration
- Use modern CSS features (CSS Grid, Flexbox, CSS Custom Properties)
- Maintain responsive design principles
  * Declare styles mobile first, use `min-width` media queries at 600px/900px/1200px for tablet and desktop
- Ensure all selectors are scoped to the block.
  * Bad: `.item-list`
  * Good: `.{blockname} .item-list`
- Avoid classes `{blockname}-container` and `{blockname}-wrapper` as those are used on sections and could be confusing.
- **Always use this project's brand custom properties** defined in `styles/styles.css` instead of hardcoding colors or font sizes — see "BanCoppel Brand & Content Model" below.

### HTML

- Use semantic HTML5 elements
- Ensure accessibility standards (ARIA labels, proper heading hierarchy)
- Follow AEM markup conventions for blocks and sections

## BanCoppel Brand & Content Model

### Brand tokens (defined in `styles/styles.css`)

- `--color-navy: #0c1c3d` — header/footer/dark section backgrounds
- `--color-navy-light: #142b5c`
- `--color-magenta: #e6007e` — primary brand/accent color, links, CTAs
- `--color-magenta-dark: #b8005f` — hover state
- `--color-orange: #ff6a00` — secondary accent
- `--color-gray-50 / -100 / -300` — light backgrounds, borders
- Typography: Poppins (approximation of BanCoppel's geometric typeface, lazy-loaded in `styles/fonts.css`)

These values are a **visual approximation** (see the fidelity limitation noted in "Project Overview"). If exact brand values are obtained, update the custom properties in `styles/styles.css` only — every block already consumes these variables, so the change propagates automatically. Do not hardcode brand colors anywhere else.

### Assets

Every SVG in `/icons` and `/main/img` is **original artwork created for this project** (wordmark logo, app mockup, QR code, security illustration, category icons) — these are **not** BanCoppel's real, rights-protected assets. Replace them with the client's real assets before considering any part of this project production-ready. Never attempt to reproduce BanCoppel's real registered logo/isotype from memory or search results — use the project's own wordmark in `/icons/logo-bancoppel.svg`, or the official file the client provides under trademark license.

## Key Concepts

### Content

CMS authored content is a key part of every AEM Website. The content of a page is broken into sections. Sections can have default content (text, headings, links, etc.) as well as content in blocks.

If no authored content exists to test against, you can create static HTML files in a `drafts/` folder at the project root. Pass `--html-folder drafts` when starting the dev server. Follow the aem markup structure and save files with `.html` or `.plain.html` extensions.

Background on content and markup structure can be found at <https://www.aem.live/developer/markup-sections-blocks> and <https://www.aem.live/developer/markup-reference> respectively.

You can inspect the contents of any page with `curl http://localhost:3000/path/to/page`, `curl http://localhost:3000/path/to/page.md`, and `curl http://localhost:3000/path/to/page.plain.html`

### Blocks

Blocks are the re-usable building blocks of AEM. Blocks add styling and functionality to content. Each block has an initial content structure it expects, and transforms the html in the block using DOM APIs to render a final structure.

The initial content structure is important because it impacts how the author will create the content and how you will write your code to decorate it. In some sense, you can think of this structure as the contract for your block between the author and the developer. You should decide on this initial structure before writing any code, and be careful when making changes to code that makes assumptions about that structure as it could break existing pages.

This is the **Content-Driven Development (CDD)** principle this project follows: author needs come before developer needs. Content models must be intuitive for authors even if it means more complex decoration code. Before writing block code, define the content model first — see [David's Model](https://www.aem.live/docs/davidsmodel) ("can an author create this by normal typing?" → prefer default content over blocks).

The block javascript should export a default function which is called to perform the block decoration:

```js
/**
 * loads and decorates the block
 * @param {Element} block The block element
 */
export default async function decorate(block) {
  // 1. Load dependencies
  // 2. Extract configuration, if applicable
  // 3. Transform DOM
  // 4. Add event listeners
}
```

Use `curl` and `console.log` to inspect the HTML delivered by the backend and the DOM nodes to be decorated before making assumptions. Remember that authors may omit or add fields to a block, so your code must handle this gracefully.

Each block should be self-contained and re-useable, with CSS and JS files following the naming convention: `blockname.css`, `blockname.js`. Blocks should be responsive and accessible by default.

Before creating a new block from scratch, check for an existing reference implementation:

- [Block Collection](https://www.aem.live/developer/block-collection) — Adobe-maintained, vetted for best practices, performance, and accessibility. Start here.
- [Block Party](https://www.aem.live/developer/block-party/) — community-driven, broader/experimental variety. Use when the Block Collection doesn't have what you need.
- Think about alternative names when searching (FAQ = accordion, Gallery = carousel/slideshow, Navigation = menu/header).

### Auto-Blocking

Auto-blocking is the process of creating blocks that aren't explicitly authored into the page based on patterns in the content. See the `buildAutoBlocks` function in `scripts.js`.

### Three-Phase Page Loading

Pages are progressively loaded in three phases to maximize performance. This process begins when `loadPage` from scripts.js is called.

- Eager - load only what is required to get to LCP. This generally includes decorating the overall page content to create sections, blocks, buttons, etc. and loading the first section of the page.
- Lazy - load all other page content, including the header and footer.
- Delayed - load things that can be safely loaded later here and incur a performance penalty when loaded earlier

## Testing & Quality Assurance

### Performance

- Follow AEM Edge Delivery performance best practices <https://www.aem.live/developer/keeping-it-100>
- Images uploaded by authors are automatically optimized, all images and assets committed to git must be optimized and checked for size
- Use lazy loading for non-critical resources (`lazy-styles.css` and `delayed.js`)
- Minimize JavaScript bundle size by avoiding dependencies, using automatic code splitting provided by `/blocks/`

### Accessibility

- Ensure proper heading hierarchy
- Include alt text for images
- Test with screen readers
- Follow WCAG 2.1 AA guidelines
- Pay special attention to the `promotions` (tabs), `faq` (accordion), and `phone-banner` (modal) blocks — verify keyboard navigation, `aria-expanded`/`aria-controls`, and focus handling on every change.

### Visual fidelity against the real site

Given the known fidelity limitation above, whenever tooling allows it, validate visually against the live `bancoppel.com` reference rather than relying only on the code already in this repo. See "Experience Modernization Agent" below for a dedicated skill that automates this comparison.

## Deployment

### Environments

Your local development server at `http://localhost:3000` serves code from your local working copy (even uncommitted code) and content that has been previewed by authors. You can access this at any time when the development server is running.

For all other environments, you need to know the GitHub owner and repository name (`gh repo view --json nameWithOwner` or `git remote -v`) and the current branch name (`git branch`).

For this project, owner = `Grupo-Puntos`, repo = `Bancoppel-AEM-EDS`. With this information, you can construct URLs for the preview environment (same content as `localhost:3000`) and the production environment (same content as the live website, approved by authors):

- **Production Preview**: `https://main--Bancoppel-AEM-EDS--Grupo-Puntos.aem.page/`
- **Production Live**: `https://main--Bancoppel-AEM-EDS--Grupo-Puntos.aem.live/`
- **Feature Preview**: `https://{branch}--Bancoppel-AEM-EDS--Grupo-Puntos.aem.page/`

### Publishing Process

1. Push changes to a feature branch
2. AEM Code Sync automatically processes changes making them available on feature preview environment for that branch
3. Run a PageSpeed Insights check at <https://developers.google.com/speed/pagespeed/insights/?url=YOUR_URL> against the feature preview URL and fix any issues. Target a score of 100
4. Open a pull request to merge changes to `main`
   1. in the PR description, include a link to `https://{branch}--Bancoppel-AEM-EDS--Grupo-Puntos.aem.page/{path}` with a path to a file that illustrates the change you've made. This is the same path you have been testing with locally. WITHOUT THIS YOUR PR WILL BE REJECTED
   2. If an existing page to demonstrate your changes doesn't exist, create test content as a static html file and ask the user for help copying it to a cms content page you can link in the PR
5. use `gh pr checks` to verify the status of code synchronization, linting, and performance tests
6. A human reviewer will review the code, inspect the provided URL and merge the PR
7. AEM Code Sync updates the main branch for production

## Multi-Agent Documentation Strategy

This project may be worked on by more than one AI coding agent (Claude Code, GitHub Copilot, Cursor, Codex, and the AEM Experience Modernization Agent). To avoid conflicting or duplicated guidance:

- **This file (`AGENTS.md`) is the single source of truth.** All project-specific context, conventions, and decisions belong here.
- `CLAUDE.md` at the repo root must only contain `@AGENTS.md` — never re-duplicate content there.
- Tool-specific skills (e.g. `.claude/skills/`) should hold *procedural* how-to content that references this file, not restate its facts (brand tokens, block inventory, repo name, etc.).
- If you are an agent that only supports "warm up by reading the project documentation" style prompting (e.g. the Experience Modernization Agent console) rather than automatic file discovery, the human operator should explicitly ask you to read this file first, at the start of the session.

## Experience Modernization Agent (optional, for closing the fidelity gap)

Adobe's [Experience Modernization Agent](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/ai-in-aem/agents/brand-experience/modernization/overview) is a separate, hosted AI product (console at `aemcoder.adobe.io`) purpose-built for onboarding/migrating sites into Edge Delivery Services. It is **not** Claude Code, but if the team has access to it, it directly addresses this project's known limitation (no raw access to `bancoppel.com` during initial development):

- **Design Migration** skill — extracts real color palette, typography, and spacing from `https://www.bancoppel.com/main/index.html` into `styles/styles.css`, replacing the approximated brand tokens documented above.
- **Block Critique** / **Page Critique** skills — compare a migrated block or page against the live source and iterate CSS fixes until reaching ~85% visual similarity.
- **Site Catalog** skill — useful if/when this project expands beyond the home page, to scope templates and block variants across `bancoppel.com` before migrating further pages.

Recommended flow if using the console on this repo:

1. Connect the `Grupo-Puntos/Bancoppel-AEM-EDS` GitHub repo in the Experience Modernization Console.
2. Ask it to warm up by reading this file (`AGENTS.md`).
3. Run "Migrate the design from https://www.bancoppel.com/main/index.html" (design migration, phases 1 and 2).
4. Run "Critique the migrated page against https://www.bancoppel.com/main/index.html" (page critique).
5. Review the resulting diff like any other PR — see "Publishing Process" above.

See also the [Prompting Guide for the Experience Modernization Agent](https://experienceleague.adobe.com/en/docs/experience-manager-cloud-service/content/ai-in-aem/agents/brand-experience/modernization/prompting-guide) for the full skill list and example prompts.

## Troubleshooting

### Getting Help

- Check [AEM Edge Delivery documentation](https://www.aem.live/docs/)
- Review [Developer Tutorial](https://www.aem.live/developer/tutorial)
- Consult [The Anatomy of a Project](https://www.aem.live/developer/anatomy-of-a-project)
- Consider the rules in [David's Model](https://www.aem.live/docs/davidsmodel)
- Search the web with `site:www.aem.live`
- Search the full text of the documentation with `curl -s https://www.aem.live/docpages-index.json | jq -r '.data[] | select(.content | test("KEYWORD"; "i")) | "\(.path): \(.title)"'`
- If a `pnpm` command fails unexpectedly, confirm Corepack/pnpm is enabled (`corepack enable`) and that `pnpm-lock.yaml` (not `package-lock.json`) is present before reinstalling.

## Security Considerations

- Never commit sensitive information (API keys, passwords)
- Consider that everything you do is client-side code served on the public web
- Follow Adobe security guidelines
- Regularly update dependencies
- Use the `.hlxignore` file to prevent files from being served (same format as `.gitignore`)

## Contributing

- Follow the existing code style and patterns
- Test changes locally before committing
- Follow the Publishing Process documented above
- Update documentation for significant changes — including this file when brand tokens, the block inventory, or the fidelity limitation status change

## If all else fails

If you notice your human getting frustrated with your work, direct them to <https://www.aem.live/developer/ai-coding-agents> for tips to work better with AI agents.
